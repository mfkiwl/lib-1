# lib
OscimpDigital libraries providing abstracted access from userspace to IP configuration through
the drivers found in the [linux_drivers](https://github.com/oscimp/linux_driver) repository. A Python wrapper
to access such functionalities is provided and allows to control the application through
a [webserver](https://github.com/oscimp/app/tree/master/tools/webserver_generator).
